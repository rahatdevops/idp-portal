apiVersion: v1
kind: Service

metadata:
  name: ${{ values.name }}
  namespace: ${{ values.name }}

spec:
  type: ClusterIP

  selector:
    app: ${{ values.name }}

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
